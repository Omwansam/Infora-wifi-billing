# PostgreSQL queries for Infora billing (standard FreeRADIUS schema + hard-delete deprovision)

safe_characters = "@abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789.-_: /"

authorize_check_query = "\
    SELECT id, username, attribute, value, op \
    FROM radcheck \
    WHERE lower(username) = lower('%{SQL-User-Name}') \
    ORDER BY id"

authorize_reply_query = "\
    SELECT id, username, attribute, value, op \
    FROM radreply \
    WHERE lower(username) = lower('%{SQL-User-Name}') \
    ORDER BY id"

authorize_group_check_query = "\
    SELECT id, groupname, attribute, value, op \
    FROM radgroupcheck \
    WHERE groupname = '%{SQL-Group}' \
    ORDER BY id"

authorize_group_reply_query = "\
    SELECT id, groupname, attribute, value, op \
    FROM radgroupreply \
    WHERE groupname = '%{SQL-Group}' \
    ORDER BY id"

group_membership_query = "\
    SELECT groupname \
    FROM radusergroup \
    WHERE lower(username) = lower('%{SQL-User-Name}') \
    ORDER BY priority"

accounting {
    reference = "%{tolower:type.%{Acct-Status-Type}}"

    type {
        accounting-on {
            query = "\
                UPDATE radacct \
                SET acctstoptime = NOW(), acctterminatecause = '%{Acct-Terminate-Cause}' \
                WHERE acctstoptime IS NULL AND nasipaddress = '%{NAS-IP-Address}'"
        }

        accounting-off {
            query = "\
                UPDATE radacct \
                SET acctstoptime = NOW(), acctterminatecause = '%{Acct-Terminate-Cause}' \
                WHERE acctstoptime IS NULL AND nasipaddress = '%{NAS-IP-Address}'"
        }

        start {
            # customer_id / isp_id are resolved here, at insert time.
            #
            # Without them every real session lands unattributed, and because the
            # app scopes almost everything by isp_id (dashboard counts, reports,
            # the FUP monitor) a live subscriber is invisible everywhere — the
            # only "online user" you see is whatever demo row is in the table.
            #
            # coalesce(radius_login, email) mirrors radius_username() in
            # services/radius_provisioning.py: the operator-set login when there
            # is one, otherwise the email. Keep the two in step.
            #
            # A miss leaves NULL rather than failing the insert — accounting must
            # never be lost because a subscriber was renamed or deleted;
            # services/session_tracking.link_unattributed_sessions() backfills.
            query = "\
                INSERT INTO radacct \
                    (acctsessionid, acctuniqueid, username, realm, nasipaddress, \
                     nasportid, nasporttype, acctstarttime, acctupdatetime, \
                     acctsessiontime, acctauthentic, connectinfo_start, \
                     acctinputoctets, acctoutputoctets, calledstationid, \
                     callingstationid, servicetype, framedprotocol, framedipaddress, \
                     customer_id, isp_id) \
                VALUES \
                    ('%{Acct-Session-Id}', '%{Acct-Unique-Session-Id}', \
                     '%{SQL-User-Name}', '%{Realm}', '%{NAS-IP-Address}', \
                     '%{NAS-Port}', '%{NAS-Port-Type}', NOW(), NOW(), \
                     0, '%{Acct-Authentic}', '%{Connect-Info}', \
                     0, 0, '%{Called-Station-Id}', \
                     '%{Calling-Station-Id}', '%{Service-Type}', \
                     '%{Framed-Protocol}', '%{Framed-IP-Address}', \
                     (SELECT c.id FROM customers c \
                        WHERE lower(coalesce(c.radius_login, c.email)) \
                              = lower('%{SQL-User-Name}') LIMIT 1), \
                     (SELECT c.isp_id FROM customers c \
                        WHERE lower(coalesce(c.radius_login, c.email)) \
                              = lower('%{SQL-User-Name}') LIMIT 1))"
        }

        interim-update {
            query = "\
                UPDATE radacct \
                SET acctupdatetime = NOW(), \
                    acctsessiontime = '%{Acct-Session-Time}', \
                    acctinputoctets = '%{Acct-Input-Octets}', \
                    acctoutputoctets = '%{Acct-Output-Octets}' \
                WHERE acctuniqueid = '%{Acct-Unique-Session-Id}'"
        }

        stop {
            query = "\
                UPDATE radacct \
                SET acctstoptime = NOW(), \
                    acctsessiontime = '%{Acct-Session-Time}', \
                    acctinputoctets = '%{Acct-Input-Octets}', \
                    acctoutputoctets = '%{Acct-Output-Octets}', \
                    acctterminatecause = '%{Acct-Terminate-Cause}', \
                    connectinfo_stop = '%{Connect-Info}' \
                WHERE acctuniqueid = '%{Acct-Unique-Session-Id}'"
        }
    }
}
