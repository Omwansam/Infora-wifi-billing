# Place Cloudflare Origin Certificate files here (Full strict mode):
#   origin.pem  — certificate
#   origin.key  — private key
#
# Generate in Cloudflare → SSL/TLS → Origin Server → Create Certificate
# Then uncomment the HTTPS server block in config/nginx/conf.d/billing.conf
